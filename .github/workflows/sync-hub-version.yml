name: Sync hub version from client release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Read latest client version
        id: release
        run: |
          set -euo pipefail
          VERSION=""
          if [ -f q8mNx/version.json ]; then
            VERSION="$(jq -r '.version // empty' q8mNx/version.json)"
          fi
          if [ -z "$VERSION" ]; then
            VERSION="$(curl -fsSL -H "User-Agent: Hub-Sync" \
              https://jaieoakih.github.io/asngdajwk-plugins/q8mNx/version.json \
              | jq -r '.version // empty')"
          fi
          if [ -z "$VERSION" ]; then
            echo "::error::Could not read client version from q8mNx/version.json or public Pages feed"
            exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Latest client version: $VERSION"

      - name: Bump runelite.version if needed
        run: |
          set -euo pipefail
          CURRENT="$(tr -d '\n\r' < runelite.version)"
          CLIENT_VERSION="${{ steps.release.outputs.version }}"
          if [[ "$CLIENT_VERSION" =~ ^([0-9]+\.[0-9]+\.[0-9]+)\.[0-9]+$ ]]; then
            HUB_VERSION="${BASH_REMATCH[1]}"
          else
            HUB_VERSION="$CLIENT_VERSION"
          fi
          echo "Client version: $CLIENT_VERSION"
          echo "Hub manifest version: $HUB_VERSION"
          if [ "$CURRENT" = "$HUB_VERSION" ]; then
            echo "Hub already on $HUB_VERSION"
            exit 0
          fi
          echo "$HUB_VERSION" > runelite.version
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add runelite.version
          git commit -m "Auto-sync hub version to $HUB_VERSION (client $CLIENT_VERSION)"
          git push origin HEAD
